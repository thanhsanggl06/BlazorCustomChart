namespace BlazorReportTest.Components;

/// <summary>
/// Display mode toggled at the top-right of the monthly bar chart, shared by both the
/// MudBlazor (SVG) and Chart.js chart implementations.
/// </summary>
public enum BarChartMode
{
    YearComparison,
    TypeBreakdown
}
